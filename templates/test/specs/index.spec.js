import sum from '../src/js/index.js';

describe('ES6 Index', () => {
  beforeEach(() => {
  });

  afterEach(() => {

  });

  it('should return sum = 20', () => {
    expect(sum).to.equal(20);
  });
});
